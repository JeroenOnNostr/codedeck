/target
/android/build
# Autogenerated by Tauri on build (regenerated from the SDK); don't track.
/android/.tauri
# The mesh engine .so is cross-compiled by the gradle buildMeshEngineArm64 task from the
# sibling nostr-vpn crate — a build artifact, not source. ~20MB; never commit it.
/android/src/main/jniLibs
